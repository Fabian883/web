function Logo() {
    return(
        <img 
            className={`w-${width || 20}) h-${height || 20}`}
            alt="Logo"
            src="https://bit.ly/logo-ci0137"
        />
    );
}

export default Logo;